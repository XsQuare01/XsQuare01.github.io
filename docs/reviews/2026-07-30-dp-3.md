schema_version: review-report/v2
target: dp-3
generated_at: 2026-07-30
strict: not-recorded
summary: 🔴 0 · 🟡 3 · 🟢 6

## Findings

### 🟡 [L2] src/content/posts/dp-3.md:19

- severity: 🟡
- source: L
- rule_id: L2
- location: src/content/posts/dp-3.md:19
- quote: (더 나가면) 어떤 괄호 순서인지 **복원**, 복잡도
- message: 개요의 항목 구분이 실제 본문 구성과 어긋남 — 복잡도는 독립된 `비용 따지기` 절에서 핵심 알고리즘 분석으로 다루지만, 개요에서는 원문 밖 확장인 복원과 함께 `(더 나가면)` 항목에 묶여 있다. 실제 글의 범위와 절 순서를 미리 안내하는 기능이 흐려진다.
- recommendation: 복잡도와 복원을 별도 항목으로 나누고, `(더 나가면)` 표시는 복원에만 적용하길 권한다.
- gate_effect: warn

### 🟡 [L4] public/images/dp-3/table-fill.svg:68

- severity: 🟡
- source: L
- rule_id: L4
- location: public/images/dp-3/table-fill.svg:68
- quote: 채우는 순서 / ① 길이 1 (기본값) / ② 길이 2 / ③ 길이 3 · 정답
- message: 표의 세로 화살표가 대각선 전체의 채움 순서를 충분히 나타내지 못함 — 본문과 SVG 상단 설명은 같은 길이의 모든 구간을 대각선 단위로 채운다고 말하지만, `채우는 순서` 화살표는 열 `j=3`의 `M[3,3]`, `M[2,3]`, `M[1,3]`만 세로로 잇는다. 이 표시만 보면 각 길이의 전체 대각선이 아니라 한 열만 순서대로 채우는 것으로 읽힐 수 있다.
- recommendation: 길이 1, 2, 3에 해당하는 대각선 전체를 단계별 띠나 화살표로 표시하거나, 현재 세로 화살표가 `j=3` 열의 대표 셀만 가리킨다고 명시하길 권한다.
- gate_effect: warn

### 🟡 [L7] src/content/posts/dp-3.md:103

- severity: 🟡
- source: L
- rule_id: L7
- location: src/content/posts/dp-3.md:103
- quote: int matrixChain(const vector<int>& d, int n)` / `d[i-1]*d[k]*d[j]
- message: 비용 자료형의 유효 범위가 제시되지 않음 — 차원이나 최대 비용의 범위를 제한하지 않은 채 비용과 세 차원의 곱을 `int`로 계산한다. 예시 값은 안전하지만, 일반 입력에서는 스칼라 곱셈 횟수와 누적 비용이 32비트 정수 범위를 넘어 점화식의 최솟값이 잘못될 수 있다.
- recommendation: 의사코드의 전제에 안전한 입력 범위를 밝히거나, 비용 표와 반환형을 `long long`, 초깃값을 `LLONG_MAX`, 곱을 `1LL*d[i-1]*d[k]*d[j]`처럼 계산하길 권한다.
- gate_effect: warn

### 🟢 [L1] src/content/posts/dp-3.md:11

- severity: 🟢
- source: L
- rule_id: L1
- location: src/content/posts/dp-3.md:11
- quote: 이번엔 결정의 대상이 원소가 아니라 **구간**이다. *이 구간의 마지막 곱셈을 어디서 하는가.*
- message: 문체와 문장 규칙 확인 — 검토 완료, 이슈 없음. 평서체와 강조가 절제되어 있고 문두 접속어, 지시어, 의존 명사, 중복 표현이 설명을 방해할 정도로 쓰이지 않았다. 제목과 계산 예시의 줄표도 구분 기능에 그쳐 남발로 보이지 않는다.
- recommendation: 현재 문체를 유지한다.
- gate_effect: info

### 🟢 [L3] src/content/posts/dp-3.md:43

- severity: 🟢
- source: L
- rule_id: L3
- location: src/content/posts/dp-3.md:43
- quote: $M_i \times \cdots \times M_j$ 를 곱하는 데 드는 최소 곱셈 횟수를 $M[i,j]$ 로 정의한다.
- message: 용어와 표기 일관성 확인 — 검토 완료, 이슈 없음. 행렬은 아래첨자 표기 `M_i`, 부분 문제의 최소 비용은 대괄호 표기 `M[i,j]`, 차원은 `d_{i-1}, d_k, d_j`, 분할점은 `k`로 일관되게 구분된다.
- recommendation: 현재 용어와 표기 규약을 유지한다.
- gate_effect: info

### 🟢 [L4] public/images/dp-3/cost-compare.svg:23

- severity: 🟢
- source: L
- rule_id: L4
- location: public/images/dp-3/cost-compare.svg:23
- quote: ① 3·2·4 = 24` / `② 3·4·2 = 24` / `24 + 24 = 48` / `16 + 12 = 28
- message: SVG의 수치, 레이블, 본문 참조 확인 — 검토 완료, 이슈 없음. `cost-compare.svg`의 48과 28, `split.svg`의 `M[i,k]`, `M[k+1,j]`, `dᵢ₋₁ dₖ dⱼ`, `table-fill.svg`의 0, 24, 16, 28은 본문, 대체 텍스트, 점화식과 일치한다. 별도 L4 경고는 `table-fill.svg`의 채움 순서 표현 범위에 한정된다.
- recommendation: 수치와 레이블의 현재 정합성을 유지한다.
- gate_effect: info

### 🟢 [L5] src/content/posts/dp-3.md:2

- severity: 🟢
- source: L
- rule_id: L5
- location: src/content/posts/dp-3.md:2
- quote: title: "동적 계획법 ③ — 구간을 어디서 자를 것인가"
- message: 제목과 description 적합성 확인 — 검토 완료, 이슈 없음. 제목은 행렬 사슬의 최상위 분할 결정을 드러내며, description은 48 대 28 예시, 점화식, 길이순 표 채움, `O(n³)`, dp-2와의 연결을 실제 본문 범위에 맞게 요약한다. category와 difficulty도 허용 enum 및 설계 스펙과 일치한다.
- recommendation: 현재 메타데이터를 유지한다.
- gate_effect: info

### 🟢 [L6] src/content/posts/dp-3.md:47-50,116,124-134, docs/superpowers/specs/2026-07-30-dp-3-design.md:1-40

- severity: 🟢
- source: L
- rule_id: L6
- location: src/content/posts/dp-3.md:47-50,116,124-134, docs/superpowers/specs/2026-07-30-dp-3-design.md:1-40
- quote: 여기서부터는 강의 노트에 없는 확장이다.
- message: 노션 원문 대조 완료 — 줄기 보존, 확장은 명시 — 검토 완료, 이슈 없음. 노션 "Dynamic programming 2"(Matrix multiplication 절)와 "Dynamic programming 3"을 `notion-fetch`로 가져와 대조했다. 원문 줄기 — 곱셈 순서에 따라 비용이 달라짐, 예시 `(3×2)(2×4)(4×2)`의 48 대 28, 재귀식 `M[i,j]=min_k(M[i,k]+M[k+1,j]+d_{i-1}d_k d_j)`, 기저 `M[i,i]=0`, 구간 길이 순 채우기, 정답 `M[1,n]` — 이 모두 보존됐다. 표기는 노션의 rough draft(DP2, `d_i d_{k+1} d_{j+1}`)와 정제형(DP3, `d_{i-1}d_k d_j`) 중 표준 규약으로 통일했고 예시 수치가 원문과 일치한다. 원문 밖 확장(정리·증명의 엄밀 조판, 괄호화 복원)은 132행에서 "강의 노트에 없는 확장"으로 명시된다. 주목할 divergence: 노션이 복잡도를 "O(n²)"로 적은 대목을 본문은 `O(n³)`으로 바로잡되, 노션 수치가 부분 문제의 *개수*였음을 124~126행에서 설명해 근거를 남긴다. 이는 원문 오류의 의도적·설명된 정정이며 자의적 추가(hallucination)가 아니다.
- recommendation: 수정 없음. provenance 기록을 유지한다.
- gate_effect: info

### 🟢 [L7] src/content/posts/dp-3.md:116

- severity: 🟢
- source: L
- rule_id: L7
- location: src/content/posts/dp-3.md:116
- quote: $M[1,3]=\min(0+16+12,\;24+0+24)=\min(28,48)=28$
- message: 계산, 증명, 후보 분할, 복잡도 확인 — 검토 완료, 이슈 없음. 독립 계산에서 왼쪽 괄호화는 `3·2·4=24`, `3·4·2=24`, 합계 48이고 오른쪽 괄호화는 `2·4·2=16`, `3·2·2=12`, 합계 28이다. 따라서 `M[1,2]=24`, `M[2,3]=16`, `M[1,3]=28`이 맞다. 증명은 최적 괄호화에서 점화식 최솟값으로 가는 하계와 각 분할 후보가 실제 괄호화를 구성한다는 상계를 모두 보인다. 모든 완전 괄호화는 유일한 최상위 분할점 `k`를 가지므로 `i ≤ k < j` 후보는 빠짐없이 전체를 덮고 서로 겹치지 않는다. 코드의 반복 횟수는 `Σ(len=2..n)(n-len+1)(len-1)=Θ(n³)`이고, `(n+1)×(n+1)` 표는 `Θ(n²)` 공간이므로 본문의 `O(n³)` 시간과 `O(n²)` 공간 주장은 맞다. 별도 L7 경고는 자료형 범위에 한정된다.
- recommendation: 점화식, 양방향 증명, 분할 범위, 복잡도 설명은 현재 형태를 유지한다.
- gate_effect: info
