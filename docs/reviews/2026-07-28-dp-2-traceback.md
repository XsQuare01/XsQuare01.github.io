# 리뷰 리포트 — dp-2-traceback

- schema_version: review-report/v2
- target: dp-2-traceback
- generated_at: 2026-07-28
- strict: false
- 대상 파일: `src/content/posts/dp-2-traceback.md`
- 에셋: `public/images/dp-2/traceback.svg`

## 결정적 검사 (D)

`python .claude/review_post.py --write-reports src/content/posts/dp-2-traceback.md`

발견 사항 없음 ✅ (D1~D13 전부 통과)

## LLM 비평 (L1~L7)

### 발견 사항

#### 🟡 [L1] 문두 접속어 "하지만" 3회 반복

- 심각도: 🟡 권장
- 출처: L1 (문체 / 바른 문장 쓰기)
- rule_id: L1
- 위치: `src/content/posts/dp-2-traceback.md:11`, `:29`, `:101`
- quote:
  - L11: "…최대 일급 $15$를 구했다. **하지만** 표에는 숫자만 있다."
  - L29: "…값만 비교해서 큰 쪽을 고른다. **하지만** 그 비교가 일어난 자리…"
  - L101: "…$O(1)$ 공간으로 줄일 수 있다. **하지만** 표를 지우는 순간…"
- message: `docs/writing-rules.md`의 최우선 규칙(문두 접속어 최소화)에 비추어, 한 편에서 문두 "하지만"이 3회 반복된다. 세 자리 모두 대조 전환이지만 문장 순서·인과로 대체하거나 삭제해도 의미가 유지된다. 이전 리뷰에서도 동일하게 "하지만" 3회가 지적된 이력이 있어 소급 적용이 필요하다.
- recommendation: 세 곳 중 최소 2곳을 접속어 없이 다시 쓴다. 예) L11 "…$15$를 구했다. 그러나 표에 남은 것은 숫자뿐이다." 대신 "…$15$를 구했다. 표에 남은 것은 숫자뿐, **어느 날을 골랐는지**는 어디에도 없다."처럼 대조를 문장 구조로 흡수. L29·L101도 앞 문장 끝과 이어 붙여 전환을 자연화.
- gate_effect: warn

### 검토 완료 — 이슈 없음 (coverage)

| rule | 항목 | 결과 |
|------|------|------|
| L2 | 설명 흐름·명료성 | ✅ 값↔구성 구분 → 거꾸로 읽기 규칙 → 예시 되짚기 → 코드 → 동점/공간 → 마치며 순으로 군더더기 없이 전개. 손 되짚기와 코드의 인덱스 흡수(L92)를 명시적으로 연결해 이해 단절 없음. |
| L3 | 용어·어체 일관성 | ✅ 되짚기/경유/구성/값, `r[i][1]`(함)·`r[i][0]`(안 함) 표기 일관. 본문 "근무/일함/쉼"과 SVG "일한 날"은 표 라벨 "함/안 함"과 동일 개념으로 혼동 없음. 전체 ~다 평서체 유지. |
| L4 | SVG ↔ 본문 일치 | ✅ traceback.svg의 채운 표(안 함 0·3·5·9 / 함 3·5·9·15)가 본문 $r$ 값과 일치. 역방향 화살표 `r[4][1]=15 → r[2][1]=5`, `r[3][0]=5`는 "경유" 라벨로만 표시하고 2일·4일만 하이라이트. 본문 예시(L57)도 3일을 별도 선택 정지점이 아니라 "경유"로만 서술 → 완전 일치. 결과 "{2일,4일}, 합 5+10=15"도 본문과 동일. 세로 클리핑 없음(콘텐츠 최대 y≈346 < viewBox 430). |
| L5 | 제목·description 적합성 | ✅ 제목 "추가 설명 — 어느 날을 골랐는지 되짚기"가 내용과 부합. description이 a=[3,5,6,10]·{2,4}·합 15·`reconstruct()`·동점을 정확히 요약. frontmatter(category algorithm / difficulty 고급 / numbered true) 정합. |
| L6 | 소스 자료 충실성 | ✅ 도입부(L11)와 콜아웃에서 "강의 노트 밖 확장"임을 명시. 일반 DP 스킬(해 복원)을 노션 노트 밖 확장으로 다룬다는 점이 본문에 기재됨. (참고: 이 세션에 notion-fetch/notion-search 도구는 사용 가능하나, 페이지가 스스로 노트 밖 확장임을 선언하므로 원노트 대조는 불요.) |
| L7 | 논증·정확성 | ✅ 아래 검산 참조. `reconstruct()` 손 실행 결과 [2,4], 역추적 규칙·산술·동점 처리 모두 정확. |

### L7 검산 상세

- 표: `r[1]=(0,3), r[2]=(3,5), r[3]=(5,9), r[4]=(9,15)`, `a=[3,5,6,10]`.
- `reconstruct()` 손 실행:
  1. `i=4`, `worked = r[4][1](15) > r[4][0](9) = true` → `days=[4]`, `i-=2 → 2`, `worked = r[2][1](5) > r[2][0](3) = true`
  2. `i=2`, worked=true → `days=[4,2]`, `i-=2 → 0`, 갱신 생략
  3. `i=0`, `while(i>=1)` 거짓 → 종료
  4. `reverse` → **[2, 4]** ✅
- 역추적 규칙 정확성: worked면 $i$일 선택 + $i{-}1$일 강제 휴무이므로 `i-=2`, worked가 거짓이면 경유로 `i-=1`. 매 도착 열에서 `r[i][1] > r[i][0]`로 큰 쪽을 고르는 방식이 본문 "표를 거꾸로 읽기"의 셀-단위 서술(worked → `r[i-1][0]`; not-worked → `max(r[i-1][0], r[i-1][1])`)과 동치. 경유 한 칸(`r[3][0]`)을 코드가 인덱스로 흡수한다는 설명(L92)도 정확.
- 산술: 선택 {2,4} → `a[2]+a[4] = 5+10 = 15`, 표의 `r[4][1]=15`와 일치 ✅
- 동점 처리: `>` vs `>=`가 동점 시 갈래를 가른다는 서술 정확. 동점에서는 합은 같고 선택 집합만 달라지며 "유일 정답"이 아니라는 콜아웃 설명도 옳음 ✅

## 요약: 🔴 0 · 🟡 1 · 🟢 0

---

## 후속 처리

- 🟡 [L1] 문두 "하지만" 3회(L11·L29·L101) → 커밋 `69ec1f0`에서 세 문장 모두 문두 접속어 없이 재작성, 재리뷰 ADDRESSED(파일 내 "하지만" 0건). 수식·reconstruct 코드·표 값 불변.
