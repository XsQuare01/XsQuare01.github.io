# TOC (Table of Contents) Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 글 상세 페이지의 우측 스티키 패널에 h2 기반 목차를 표시하고, 스크롤 위치에 따라 현재 섹션을 하이라이트한다.

**Architecture:** `[...slug].astro`에서 h2 headings를 추출해 `PostLayout`에 prop으로 전달하고, `PostLayout`이 `<Fragment slot="toc">`를 통해 `BaseLayout`의 `<slot name="toc" />`에 TOC 마크업을 주입한다. IntersectionObserver로 스크롤 추적을 구현한다.

**Tech Stack:** Astro v6, CSS Variables, IntersectionObserver API

---

## 파일 변경 목록

| 파일 | 작업 |
|------|------|
| `src/styles/global.css` | TOC 스타일 추가 |
| `src/layouts/BaseLayout.astro` | `.right-panel`에 `<slot name="toc" />` 추가 |
| `src/layouts/PostLayout.astro` | `headings` prop 추가, TOC Fragment + IntersectionObserver script |
| `src/pages/blog/[...slug].astro` | headings 추출 및 PostLayout에 전달 |

---

### Task 1: TOC CSS 스타일 추가

**Files:**
- Modify: `src/styles/global.css`

- [ ] **Step 1: `global.css`의 `/* Mobile */` 블록 바로 위에 TOC 스타일 추가**

```css
/* ── TOC ── */
.toc {
  padding-top: 0.5rem;
}

.toc-title {
  font-size: 0.7rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--text-dim);
  margin-bottom: 0.75rem;
}

.toc-list {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 0.35rem;
}

.toc-link {
  font-size: 0.82rem;
  color: var(--text-muted);
  display: block;
  line-height: 1.4;
  transition: color 0.15s;
}

.toc-link:hover { color: var(--text); }
.toc-link.active { color: var(--accent); font-weight: 600; }
```

- [ ] **Step 2: 빌드 확인**

```bash
npm run build
```

Expected: `✓ Completed`

- [ ] **Step 3: 커밋**

```bash
git add src/styles/global.css
git commit -m "style: TOC 스타일 추가"
```

---

### Task 2: BaseLayout에 toc named slot 추가

**Files:**
- Modify: `src/layouts/BaseLayout.astro`

- [ ] **Step 1: `.right-panel` 안의 빈 주석을 `<slot name="toc" />`으로 교체**

찾을 코드:
```astro
      <aside class="right-panel">
        <!-- 준비 중: 우측 패널 콘텐츠 -->
      </aside>
```

교체할 코드:
```astro
      <aside class="right-panel">
        <slot name="toc" />
      </aside>
```

- [ ] **Step 2: 빌드 확인**

```bash
npm run build
```

Expected: `✓ Completed`

- [ ] **Step 3: 커밋**

```bash
git add src/layouts/BaseLayout.astro
git commit -m "feat: BaseLayout right-panel에 toc named slot 추가"
```

---

### Task 3: PostLayout에 headings prop 및 TOC 마크업 추가

**Files:**
- Modify: `src/layouts/PostLayout.astro`

- [ ] **Step 1: Props 인터페이스에 `headings` 추가 및 구조분해**

기존:
```astro
interface Props {
  title: string;
  date: Date;
  description?: string;
  tags?: string[];
}

const { title, date, description, tags = [] } = Astro.props;
```

변경 후:
```astro
interface Props {
  title: string;
  date: Date;
  description?: string;
  tags?: string[];
  headings?: { slug: string; text: string }[];
}

const { title, date, description, tags = [], headings = [] } = Astro.props;
```

- [ ] **Step 2: `<BaseLayout>` 호출부에 TOC Fragment 추가**

기존:
```astro
<BaseLayout title={title} description={description}>
  <article>
    <div class="post-header">
```

변경 후:
```astro
<BaseLayout title={title} description={description}>
  {headings.length > 0 && (
    <Fragment slot="toc">
      <div class="toc">
        <div class="toc-title">목차</div>
        <ul class="toc-list">
          {headings.map((h) => (
            <li>
              <a class="toc-link" href={`#${h.slug}`}>{h.text}</a>
            </li>
          ))}
        </ul>
      </div>
    </Fragment>
  )}
  <article>
    <div class="post-header">
```

- [ ] **Step 3: `</BaseLayout>` 닫는 태그 앞에 IntersectionObserver 스크립트 추가**

`</BaseLayout>` 바로 앞(파일 맨 아래)에 추가:

```astro
<script>
  const headings = document.querySelectorAll('article h2[id]');
  const tocLinks = document.querySelectorAll('.toc-link');

  if (headings.length > 0 && tocLinks.length > 0) {
    const observer = new IntersectionObserver(
      (entries) => {
        for (const entry of entries) {
          if (entry.isIntersecting) {
            tocLinks.forEach((link) => link.classList.remove('active'));
            const active = document.querySelector(
              `.toc-link[href="#${entry.target.id}"]`
            );
            if (active) active.classList.add('active');
          }
        }
      },
      { rootMargin: '0px 0px -80% 0px' }
    );

    headings.forEach((h) => observer.observe(h));
  }
</script>
```

- [ ] **Step 4: 빌드 확인**

```bash
npm run build
```

Expected: `✓ Completed`

- [ ] **Step 5: 커밋**

```bash
git add src/layouts/PostLayout.astro
git commit -m "feat: PostLayout에 TOC 마크업 및 IntersectionObserver 스크롤 추적 추가"
```

---

### Task 4: [...slug].astro에서 headings 추출 및 전달

**Files:**
- Modify: `src/pages/blog/[...slug].astro`

- [ ] **Step 1: `render(post)`에서 headings 추출 및 h2 필터링**

기존:
```astro
const { post } = Astro.props;
const { Content } = await render(post);
```

변경 후:
```astro
const { post } = Astro.props;
const { Content, headings } = await render(post);
const h2Headings = headings.filter((h) => h.depth === 2);
```

- [ ] **Step 2: `<PostLayout>`에 `headings` prop 전달**

기존:
```astro
<PostLayout
  title={post.data.title}
  date={post.data.date}
  description={post.data.description}
  tags={post.data.tags}
>
```

변경 후:
```astro
<PostLayout
  title={post.data.title}
  date={post.data.date}
  description={post.data.description}
  tags={post.data.tags}
  headings={h2Headings}
>
```

- [ ] **Step 3: 빌드 확인**

```bash
npm run build
```

Expected: `✓ Completed`

- [ ] **Step 4: 동작 확인**

```bash
npm run dev
```

브라우저에서 `http://localhost:4321/blog/interesting-number-paradox` 열어 확인:
- 우측 패널에 "목차" 제목과 h2 항목들이 표시되는가
- 스크롤 시 현재 섹션의 목차 항목이 주황색으로 하이라이트되는가
- 목차 항목 클릭 시 해당 섹션으로 이동하는가
- 홈(`/`), 글 목록(`/blog`) 등 다른 페이지에서는 우측 패널이 비어있는가

- [ ] **Step 5: 커밋**

```bash
git add src/pages/blog/[...slug].astro
git commit -m "feat: 글 상세 페이지에 TOC headings 추출 및 전달"
```
