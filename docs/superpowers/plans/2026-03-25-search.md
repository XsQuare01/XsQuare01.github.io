# Search Feature Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fuse.js 기반 검색 모달을 추가한다 — 사이드바 버튼 또는 Ctrl+K로 열리고, 포스트를 제목/설명/태그/카테고리로 퍼지 검색한다.

**Architecture:** 빌드 시 `/search-index.json` 정적 파일을 생성하고, 런타임에 모달 최초 오픈 시 lazy fetch 후 Fuse.js로 검색한다. 모달 HTML은 BaseLayout에 추가하고, JS는 같은 파일의 `<script>` 블록에 작성한다.

**Tech Stack:** Fuse.js 7.x, Astro Content Collections API, CSS Variables (기존 다크/라이트 모드 시스템)

---

## File Map

| 파일 | 역할 |
|------|------|
| `src/pages/search-index.json.ts` | 빌드 시 검색 인덱스 JSON 생성 (신규) |
| `src/layouts/BaseLayout.astro` | 사이드바 검색 버튼 + 모달 HTML + JS (수정) |
| `src/styles/global.css` | 모달 CSS 스타일 (수정) |

---

## Task 1: fuse.js 설치

**Files:**
- Modify: `package.json`

- [ ] **Step 1: fuse.js 설치**

```bash
cd C:/Users/bhmun/XsQuare01.github.io
npm install fuse.js
```

Expected: `package.json`의 `dependencies`에 `"fuse.js": "^7.x.x"` 추가됨

- [ ] **Step 2: 빌드 확인**

```bash
npm run build
```

Expected: `dist/` 정상 생성, 에러 없음

- [ ] **Step 3: 커밋**

```bash
git add package.json package-lock.json
git commit -m "Chore: fuse.js 검색 라이브러리 설치"
```

---

## Task 2: 검색 인덱스 API Route 생성

**Files:**
- Create: `src/pages/search-index.json.ts`

- [ ] **Step 1: 파일 생성**

`src/pages/search-index.json.ts` 를 아래 내용으로 생성:

```ts
import type { APIRoute } from 'astro';
import { getCollection } from 'astro:content';

export const GET: APIRoute = async () => {
  const posts = await getCollection('posts');
  const index = posts.map((post) => ({
    id: post.id,
    title: post.data.title,
    description: post.data.description ?? '',
    tags: post.data.tags ?? [],
    category: post.data.category ?? '',
  }));
  return new Response(JSON.stringify(index), {
    headers: { 'Content-Type': 'application/json' },
  });
};
```

> `post.id`는 Astro Content Collections에서 파일명 기반 slug (예: `"rsa"`) — URL `/blog/rsa`에 대응함.
> `export const prerender = true` 불필요 — 프로젝트 기본 출력 모드가 `static`이므로 자동 처리됨.

- [ ] **Step 2: 빌드 후 인덱스 파일 확인**

```bash
npm run build
```

Expected: `dist/search-index.json` 파일 존재, 내용에 포스트 배열이 있음

```bash
cat dist/search-index.json | head -c 200
```

Expected: `[{"id":"alice-bob","title":"Alice and Bob...` 형태의 JSON 배열

- [ ] **Step 3: 커밋**

```bash
git add src/pages/search-index.json.ts
git commit -m "Feat: 검색 인덱스 JSON 엔드포인트 추가"
```

---

## Task 3: 모달 CSS 추가

**Files:**
- Modify: `src/styles/global.css`

기존 `#back-to-top` 스타일 블록 뒤에 아래 CSS를 추가한다.

- [ ] **Step 1: global.css에 모달 스타일 추가**

`src/styles/global.css`의 `#back-to-top` 관련 스타일 블록 뒤에 추가:

```css
/* ── Search Modal ── */
#search-modal {
  position: fixed;
  inset: 0;
  z-index: 1001;
  background: rgba(0, 0, 0, 0.6);
  display: flex;
  align-items: flex-start;
  justify-content: center;
  padding-top: 12vh;
  opacity: 0;
  pointer-events: none;
  transition: opacity 0.15s;
}

#search-modal.open {
  opacity: 1;
  pointer-events: auto;
}

.search-modal-box {
  width: 100%;
  max-width: 520px;
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: 12px;
  overflow: hidden;
  box-shadow: 0 24px 64px rgba(0, 0, 0, 0.4);
  transform: translateY(-8px);
  transition: transform 0.15s;
}

#search-modal.open .search-modal-box {
  transform: translateY(0);
}

.search-modal-input-row {
  display: flex;
  align-items: center;
  gap: 0.6rem;
  padding: 0.85rem 1rem;
  border-bottom: 1px solid var(--border);
}

.search-modal-icon {
  color: var(--text-dim);
  font-size: 1rem;
  flex-shrink: 0;
}

#search-input {
  flex: 1;
  background: transparent;
  border: none;
  outline: none;
  font-size: 0.95rem;
  color: var(--text);
  font-family: var(--font-sans);
}

#search-input::placeholder {
  color: var(--text-dim);
}

.search-modal-esc {
  font-size: 0.65rem;
  color: var(--text-dim);
  background: var(--bg-hover);
  border: 1px solid var(--border);
  border-radius: 4px;
  padding: 0.15rem 0.4rem;
  flex-shrink: 0;
}

#search-results {
  max-height: 360px;
  overflow-y: auto;
  padding: 0.4rem;
}

#search-results:empty {
  display: none;
}

.search-result-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 0.75rem;
  padding: 0.55rem 0.75rem;
  border-radius: 6px;
  text-decoration: none;
  color: var(--text);
  transition: background 0.1s;
}

.search-result-item:hover,
.search-result-item.focused {
  background: var(--accent-dim);
}

.search-result-title {
  font-size: 0.875rem;
  flex: 1;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.search-result-badge {
  font-size: 0.65rem;
  padding: 0.1rem 0.45rem;
  border-radius: 4px;
  background: rgba(249, 115, 22, 0.12);
  color: var(--accent);
  flex-shrink: 0;
  font-family: var(--font-mono);
}

.search-empty {
  padding: 1.25rem 1rem;
  font-size: 0.85rem;
  color: var(--text-dim);
  text-align: center;
}

/* Search button in sidebar */
#search-btn {
  display: flex;
  align-items: center;
  gap: 0.4rem;
  width: 100%;
  padding: 0.4rem 0.75rem;
  margin-bottom: 0.5rem;
  background: var(--bg-hover);
  border: 1px solid var(--border);
  border-radius: 6px;
  font-size: 0.85rem;
  color: var(--text-muted);
  font-family: var(--font-sans);
  cursor: pointer;
  transition: color 0.2s, border-color 0.2s;
  text-align: left;
}

#search-btn:hover {
  color: var(--text);
  border-color: var(--accent);
}

#search-btn-icon {
  font-size: 0.8rem;
  color: var(--text-dim);
}

#search-btn-kbd {
  margin-left: auto;
  font-size: 0.65rem;
  color: var(--text-dim);
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: 3px;
  padding: 0.1rem 0.35rem;
  font-family: var(--font-mono);
}
```

- [ ] **Step 2: 빌드 확인**

```bash
npm run build
```

Expected: 에러 없음

- [ ] **Step 3: 커밋**

```bash
git add src/styles/global.css
git commit -m "Feat: 검색 모달 CSS 스타일 추가"
```

---

## Task 4: BaseLayout에 모달 HTML + 사이드바 버튼 + JS 추가

**Files:**
- Modify: `src/layouts/BaseLayout.astro`

- [ ] **Step 1: 사이드바 검색 버튼 추가**

`src/layouts/BaseLayout.astro`에서 `<nav class="sidebar-nav">` 블록 바로 위에 검색 버튼을 추가:

```astro
    <button id="search-btn" aria-label="검색">
      <span id="search-btn-icon">🔍</span>
      검색
      <kbd id="search-btn-kbd">Ctrl K</kbd>
    </button>
```

즉, 삽입 위치:
```
[sidebar-profile]
[search-btn]       ← 여기 추가
[sidebar-nav]
[sidebar-categories]
...
```

- [ ] **Step 2: 모달 HTML 추가**

`</body>` 태그 바로 위 (기존 `<button id="back-to-top">` 아래)에 추가:

```astro
  <div id="search-modal" role="dialog" aria-modal="true" aria-label="검색">
    <div class="search-modal-box">
      <div class="search-modal-input-row">
        <span class="search-modal-icon">🔍</span>
        <input
          id="search-input"
          type="text"
          placeholder="검색어를 입력하세요..."
          autocomplete="off"
          spellcheck="false"
        />
        <span class="search-modal-esc">esc</span>
      </div>
      <div id="search-results"></div>
    </div>
  </div>
```

- [ ] **Step 3: `<script>` 블록에 검색 JS 추가**

기존 `<script>` 블록 (back-to-top + theme-toggle JS가 있는 곳)에 아래 코드를 추가:

```ts
import Fuse from 'fuse.js';

// ── 검색 모달 ──
const modal = document.getElementById('search-modal') as HTMLElement;
const searchInput = document.getElementById('search-input') as HTMLInputElement;
const searchResults = document.getElementById('search-results') as HTMLElement;

let fuse: InstanceType<typeof Fuse> | null = null;

async function loadIndex() {
  if (fuse) return;
  const res = await fetch('/search-index.json');
  const data = await res.json();
  fuse = new Fuse(data, {
    keys: [
      { name: 'title',       weight: 0.5 },
      { name: 'description', weight: 0.25 },
      { name: 'tags',        weight: 0.15 },
      { name: 'category',    weight: 0.10 },
    ],
    threshold: 0.4,
    minMatchCharLength: 2,
  });
}

function openModal() {
  modal.classList.add('open');
  searchInput.focus();
  loadIndex();
}

function closeModal() {
  modal.classList.remove('open');
  searchInput.value = '';
  searchResults.innerHTML = '';
}

document.getElementById('search-btn')?.addEventListener('click', openModal);

modal.addEventListener('click', (e) => {
  if (e.target === modal) closeModal();
});

document.addEventListener('keydown', (e) => {
  if ((e.metaKey || e.ctrlKey) && e.key === 'k') {
    e.preventDefault();
    modal.classList.contains('open') ? closeModal() : openModal();
  }
  if (e.key === 'Escape') closeModal();
});

searchInput.addEventListener('input', () => {
  const query = searchInput.value.trim();
  if (!query || !fuse) {
    searchResults.innerHTML = '';
    return;
  }

  const results = fuse.search(query, { limit: 8 });
  if (results.length === 0) {
    searchResults.innerHTML = `<div class="search-empty">결과 없음</div>`;
    return;
  }

  searchResults.innerHTML = results
    .map(({ item }) => `
      <a class="search-result-item" href="/blog/${item.id}">
        <span class="search-result-title">${item.title}</span>
        ${item.category ? `<span class="search-result-badge">${item.category}</span>` : ''}
      </a>
    `)
    .join('');

  // 키보드 내비게이션 초기화
  attachKeyNav();
});

function attachKeyNav() {
  const items = searchResults.querySelectorAll<HTMLAnchorElement>('.search-result-item');
  let idx = -1;

  // 기존 keydown 리스너 제거 후 재등록
  searchInput.onkeydown = (e) => {
    if (e.key === 'ArrowDown') {
      e.preventDefault();
      items[idx]?.classList.remove('focused');
      idx = Math.min(idx + 1, items.length - 1);
      items[idx]?.classList.add('focused');
      items[idx]?.scrollIntoView({ block: 'nearest' });
    } else if (e.key === 'ArrowUp') {
      e.preventDefault();
      items[idx]?.classList.remove('focused');
      idx = Math.max(idx - 1, 0);
      items[idx]?.classList.add('focused');
      items[idx]?.scrollIntoView({ block: 'nearest' });
    } else if (e.key === 'Enter' && idx >= 0) {
      items[idx]?.click();
    }
  };
}
```

> **주의:** `<script>` 태그에 `type` 속성이 없어야 Astro가 TypeScript로 처리함. 기존 `<script>` 블록 안에 추가하는 것임.

- [ ] **Step 4: 빌드 확인**

```bash
npm run build
```

Expected: 에러 없음, `dist/` 정상 생성

- [ ] **Step 5: 로컬 프리뷰로 동작 확인**

```bash
npm run preview
```

확인 항목:
1. 사이드바에 🔍 검색 버튼이 보임
2. 버튼 클릭 → 모달 열림
3. `Ctrl+K` → 모달 열림
4. `Esc` → 모달 닫힘
5. 검색어 입력 → 결과 표시 (제목 + 카테고리 뱃지)
6. 결과 클릭 → 해당 포스트 이동
7. `↑↓` 키로 결과 내비게이션
8. 라이트 모드 전환 후 모달 스타일 정상 확인

- [ ] **Step 6: 커밋**

```bash
git add src/layouts/BaseLayout.astro
git commit -m "Feat: 검색 모달 UI 및 Fuse.js 검색 로직 추가"
```
